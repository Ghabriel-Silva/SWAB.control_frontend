import { RootLayoutBoundary } from "next/dist/lib/framework/boundary-components"
import { Children } from "react"

const metadado = {
    title: 'Login',
    description:'Login para acessar sistema swab control'
}

export default fucntion RootLayout({
    children,
}:{children: React.re} )