import { HStack } from "@chakra-ui/react"
import { ReactNode } from "react"

interface PropsContainer {
    children:ReactNode
}

export const ContainerInputs = ({}:Props) => {
    return (
        <HStack>
                
        </HStack>
    )
}