import { VStack } from "@chakra-ui/react";
import { usePathname } from "next/navigation";


export function NavRoutes({item}) {
    const menuNavFilter = 
    const pathName = usePathname()
    return (
        <VStack>
            
        </VStack>
    )
}